import { AddCategoryModalForm, AddChatModalForm } from '@stn-ui/forms';
import { ModalNames } from '../constants';

export const modals = {
  [ModalNames.AddCategoryModal]: AddCategoryModalForm,
  [ModalNames.AddChatModal]: AddChatModalForm,
};
